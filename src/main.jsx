import React from 'react';
import ReactDOM from 'react-dom/client';
import CampaignChatInterface from '../Campaign_Chat_Interface.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CampaignChatInterface />
  </React.StrictMode>
);
